<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jalilov.mathtime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MathTime"
        tools:ignore="AllowBackup">
        <activity android:name=".activities.activities.MainActivity"/>
        <activity android:name=".activities.activities.ResultActivity" />
        <activity android:name=".activities.dividedClasses.DividedActivity" />
        <activity android:name=".activities.multiplyClasses.MultiplyActivity" />
        <activity android:name=".activities.subtractionClasses.SubtractionActivity" />
        <activity android:name=".activities.additionalClasses.AdditionalActivity"/>
        <activity android:name=".activities.activities.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5112439562846419~7254183880"/>

    </application>

</manifest>